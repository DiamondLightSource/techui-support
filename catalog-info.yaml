apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: techui-support
  title: techui-support
  description: A support package needed by techui-builder, holding symbols, widgets and gui components for css .bob file screens.
spec:
  type: repository
  owner: group:default/sscc
